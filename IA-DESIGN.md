# AI-mail 新IA/UI/UX 設計書（Milestone 1 / v1.1）

> 2026-07-25 作成。ブランチ `work/ui-overhaul`。
> v1.1: 独立レビュー3視点（機能完全性・認知ペルソナ・デザイン規律、Sonnet×3、指摘27件）を反映。主な修正 = モバイル下部グローバルバー廃止・履歴チップ全7状態化・「メンテナンス」メニュー化撤回・絵文字アイコン排除・§6拡充。
> v1.2: ユーザー提案により**商材コンテキスト（上部バーのグローバル商材スイッチャー）**を §3.4 に追加（フィルタ方式・データ分割はしない・安全ガードは全商材共通のまま）。
> v1.3: スイッチャーは登録済み全商材を自動掲載（きっかけインターン含む）と機能非破壊の保証を §3.4 に明文化（2026-07-25 ユーザー承認済み・M2着手OK）。
> 機能の正本は `UI-OVERHAUL-MAP.md`（15ページ×API契約×⚠️安全点）。本書は「同じ機能を、どこに・どう見せるか」だけを定義する。
> 手本: YouTube Studio (Web)。利用者: オーナー＋社内スタッフ（非エンジニア・デジタル習熟を前提にしない）。

---

## 0. 非交渉の維持事項（設計の外側・絶対に変えない）

1. **エンジン凍結**: `lib/`・`app/api/**` は1文字も触らない。触るのは `app/**/page.tsx`・`components/`・`app/**/layout.tsx` のみ
2. **ルートURL不変**: 全14ルートのURLはそのまま（ブックマーク・smoke テスト・proxy.ts の PUBLIC_PATHS を壊さない）
3. **API契約不変**: endpoint / method / body 形状は UI-OVERHAUL-MAP の表のとおり。既存 GET を追加で呼ぶのは可、形を変えるのは不可
4. **ページ間 state のキー不変**: sessionStorage `bulk-send-recipients` / `bulk-send-import` / `batch-generate-company-ids`、localStorage `theme` / `accent`
5. **⚠️安全ロジックの完全移植**: force/forceLow フラグ、acknowledgedWarnings 2段階、送信済/予約済除外、宛先最新1件 dedup、confirm ダイアログ（法令文言含む）、テストモード表示、beforeunload 警告 — 各ページの詳細は MAP の該当節が正本
6. **タブ＝ルート分割**を維持（SPA state 切替にしない。ブックマーク・戻るボタンのため）
7. **CSV取込は一括送信側に残す**（収集へ移さない。MAP の NAV 節に設計意図の明記あり）
8. **各マイルストーンで `npm run smoke`（70本）＋ `tsc` 全緑**を通過条件にする。ラベル・DOM構造の変更は §6 に載せた箇所のみ・1行単位で意図的に行う

---

## 1. カスタマージャーニー（2026-07-25 ユーザー確認済み）

**主動線（毎日）**: ツールを開く → 今日の状況を見る → **一括送信を流す**（テンプレ選択→宛先選択→送信/予約）
**副動線（毎日〜隔日)**: 返信・履歴チェック / 予約の消化確認
**週次**: 宛先の補充（自動収集の様子見・キーワード検索・準備中の調査）
**たまに**: テンプレ・商材・人格の手入れ、設定

設計含意:
- ホーム＝「今日の状況が3秒で分かる」＋一括送信への最短導線（ヒーローボタン1クリックで /bulk-send へ。**宛先の自動投入はしない** — 宛先選択は必ず本人が行う。「1クリック」は画面到達までの話であることを明記）
- サイドバーは**使用頻度順**（ホーム→一括送信→メール作成→宛先集め→履歴→設定）
- 「集める→作る→送る→振り返る」のパイプライン理解は**ホームの状況カードの並び**で教える（左から在庫→準備中→予約→返信）

## 2. サイトマップ（ルート不変・ラベルと所属だけ再定義）

| サイドバー | ルート | 新ラベル | 旧ラベル |
|---|---|---|---|
| 1 | `/` | ホーム | ホーム |
| 2 | `/bulk-send` | 一括送信 | 一括送信 |
| 3 | `/generate` | メール作成 | 生成 |
| 4 | `/collection`（グループ） | 宛先集め | 収集 |
| 4a | `/collection` | └ 自動収集 | 在庫と自動収集 |
| 4b | `/collection/companies` | └ 企業一覧 | 企業一覧 |
| 4c | `/collection/search` | └ キーワード検索 | キーワード検索 |
| 5 | `/history` | 履歴 | 履歴 |
| 6 | `/settings`（グループ） | 設定 | 設定 |
| 6a-e | `/settings` `/templates` `/services` `/personas` `/suppressions` | 全般 / テンプレート / 商材 / 人格 / 送信しないリスト | 同等 |
| — | `/prospect/[id]` | （ナビ外・詳細画面。シェル＝サイドバー/上部バーは表示する） | 同 |
| — | `/login` | （ナビ外・シェル無し） | 同 |

ラベル・遷移先の変更（M2実装時に実コードと突合して確定）: ①「ダッシュボード」→「ホーム」 ②「生成」→「メール作成」 ③「企業リスト」→「宛先集め」 ④サブタブ「在庫と自動収集」→「自動収集」 ⑤ナビ「設定」の遷移先を `/settings/templates` → `/settings`（全般）へ。すべて §6 に記載。
※ v1.3 までの「旧ラベル」列は nav の実ラベルと一部ズレていた（実際は ダッシュボード/企業リスト）。M2 で実コード基準に修正済み。
※ `/collection` ページ内の見出し「企業リスト」は M5 で「宛先集め」系に揃える（§6 に追記してから変更）。

## 3. 共通シェル（YouTube Studio 踏襲）

### 3.1 レイアウト
- **左サイドバー**: 3状態（YouTube Studio 同様）
  - ≥1280px: 展開 240px（アイコン＋ラベル）
  - 768〜1279px: アイコンレール 64px（ラベルはツールチップ。広いテーブルの横圧迫を防ぐ）
  - <768px: 非表示。上部バーのハンバーガー → 左からドロワー
- 現在地ハイライトは「アクセント色文字＋淡色ピル背景」。**アクティブ判定は既存 TabNav と同じ最長一致アルゴリズム**（`pathname===href || startsWith(href+'/')` の中で最長の href）を流用する
- グループ（宛先集め/設定）はクリックで先頭ページへ遷移→ページ内上部にサブタブ（既存 TabNav のルート分割を継承）
- **モバイルのグローバル下部固定バーは置かない**（レビューCRITICAL対応）。理由: /bulk-send の送信バー・/prospect の下部アクションバー等、**ページ固有の下部固定バーと同一領域を奪い合う**ため。モバイルのナビはドロワーに一本化し、下部領域はページ固有バー専用とする
- **上部バー**（56px・/login 以外の全画面）: 左=ハンバーガー（<768px のみ）＋「SalesMail」、右=商材スイッチャー（§3.4）＋テストモードバッジ（§3.2）＋**テーマ切替ボタン（現行機能・theme.spec C6 の契約を維持するため上部バーに残置**。設定>外観と併存）。検索・通知は v1 では置かない
- 上部バーは **sticky にしない**（M2実装判断: bulk-send / settings/templates の既存 `sticky top-6` パネルと競合するため。サイドバーのみ固定）
- main 最大幅 1200px、左右 padding 24px（モバイル 16px）

### 3.2 全画面共通の安全表示（新設・改善）
- **テストモードバッジ**: shell が `GET /api/settings` を1回呼び、`test_mode==='true'` なら上部バーに常時「テストモード：全メールは自分宛てに届きます」バッジ（アイコンは phosphor の Flask 等。**絵文字は使わない**）
  - **色はページ内バナーと同じ danger（赤）系トークンで統一**（同一状態に2色を使わない。淡い赤背景＋濃い赤文字＝コントラスト規律準拠）
  - **fetch 失敗時はバッジを出さないだけ**にとどめ、ナビゲーション本体には一切影響させない（エラーを投げない・ブロックしない）
  - 各ページ内の既存テストモード表示・confirm 文言分岐は**そのまま残す**（二重表示は許容。ページ側を消さない）
- **Gmail 要再認証・アクセス保護無効**: ホームのアラート枠に表示（§4.1）

### 3.3 デザイントークン
- ライトテーマ既定・白基調＋**アクセント blue 既定**。既存テーマ機構（`--primary` 系 CSS 変数・8色スウォッチ・localStorage `theme`/`accent`・OS の prefers-color-scheme 追従）は契約としてそのまま維持
- **ダークテーマも一級対応**: OS がダーク設定なら初回からダークで表示される仕様のため、新設コンポーネント（状態チップ・バッジ・状況カード等）は**必ずダーク側トークンも定義**する。M7 の視覚QAは ライト×ダーク 両方 × 全ページを対象にする
- 文字: 本文 14px 以上・行間 1.6。見出しは `text-wrap: balance`
- 色の使い分け: **青=進む操作（送信・保存・生成）／赤=取り消し・削除・危険のみ**。ただし赤の中でも**影響範囲が大きい操作（送信しないリスト・全削除）は、軽い取り消し（予約取消1件）と同じ見た目にしない**（§4.5）。状態は必ず「色＋アイコン＋日本語ラベル」の3点セット
- クリック目標 44px 以上。フォーカスリング可視。`prefers-reduced-motion` 尊重
- テーブルは `min-width:0`＋横スクロールコンテナ必須。アイコン＋テキストは span で包む
- **アイコンは phosphor-icons のみ。絵文字をアイコン代わりにしない**（本書内の記述も含め徹底）。均一カード反復・信号機配色を避ける
- **新規に書く文言は平易な日本語**（カタカナ専門語禁止）。**既存文言の変更は §6 に載せた場合のみ**（テスト互換）

### 3.4 商材コンテキスト（グローバル切替・2026-07-25 ユーザー提案で追加）

- 上部バーに**商材スイッチャー**「商材: すべて ▾」を常設（選択肢=すべて＋`GET /api/services` の一覧。SpotRight・きっかけインターン等、**設定>商材に登録済みの全商材が自動で並ぶ**＝新商材の追加にコード変更不要）。選択は localStorage `active-service`（**新規キー**。§0-4 の既存キーは不変）に保存し、全画面で共有
- **機能非破壊の保証**: スイッチャーが変えるのは「画面の初期表示（フィルタ・selectの初期値）」だけ。**APIリクエストの body を裏で書き換えることは絶対にしない**（生成に使われる商材は、常に画面上の select に見えている値）。既定「すべて」では現行と1bitも変わらない挙動
- 役割は**「全画面のフィルタ・初期選択を一括セットする作業コンテキスト」**であり、データの分割ではない。各画面の既存フィルタUIはそのまま残り、いつでも手動で上書きできる
- **反映先**:

| 画面 | 商材選択時の挙動 |
|---|---|
| ホーム | 予約中③・新しい返信④・最近の生成をその商材で集計/絞り込み。クイック生成の商材を初期選択 |
| 一括送信 | 履歴モーダル・企業一覧モーダル・生成済みメールタブの商材フィルタ初期値 |
| メール作成 | 商材 select の初期値・「企業を選ぶ」の商材フィルタ初期値 |
| 宛先集め>企業一覧 | 商材フィルタ初期値 |
| 履歴 | 商材フィルタ初期値 |

- **反映しないもの（意図的）**: 在庫サマリ①②（`/api/collection/status` はサーバ側の全体集計のため。カードに「全商材合計」と注記）／自動収集の収集元一覧（全体像を隠さない。該当商材タグの収集元に印を付けるのみ）／設定・テンプレ・人格・送信しないリスト（全商材共通の資産）
- **安全ガードに商材の概念を持ち込まない**: 送信済み判定・重複除外・送信しないリストは従来どおり会社/メール単位・**全商材共通**（同じ会社へ別商材でも連投はブロック＝エンジン仕様であり迷惑・法令対策）。商材で絞った画面でも「送信済み」バッジは全商材ベースで表示し続ける
- 既定値「すべて」＝現行と完全同一挙動。**smoke テストへの影響ゼロ**（§6 追記不要）。実装は M2 でスイッチャー本体、M4〜M6 で各画面のフィルタ初期値へ配線

## 4. ページ別ブループリント

> 各ページの「維持すべき API 契約・⚠️安全ロジック・状態受け渡し」は UI-OVERHAUL-MAP.md の該当節が正本。ここでは新レイアウトと変更点のみ。

### 4.1 ホーム `/`（役割変更: KPI羅列 → 今日の状況＋最短導線）
- **状況カード4枚**（左から、パイプライン順）: ①すぐ送れる宛先 N社（残り約N日）②準備中 N社 ③予約中 N件 ④新しい返信 N件（= `replied` のみ。商談へ昇格した件はカウントしない — 履歴の「商談」チップで見る）
  - データ源: ①②=`GET /api/collection/status`、③④=`GET /api/prospects` から集計。いずれも既存APIの追加呼び出し
  - クリック先: ①→/collection/companies（完了×未送信に絞る）②→/collection/companies?status=pending ③→/history?status=scheduled ④→/history?status=replied（クエリは新ページ側で読む。ルート不変・受け側の実装は §4.4/§4.5）
- **アラート枠**（該当時のみ・カードの上）: Gmail 要再認証（`GET /api/senders`）/ 在庫僅少・収集の自動停止（status から）/ アクセス保護無効（`GET /api/settings` の authEnabled — **ホームの取得APIに /api/settings を追加**）。1件1行・行き先ボタン付き
- **ヒーローCTA**: 「一括送信をはじめる」大ボタン（青・画面内最大）→ /bulk-send。隣に控えめに「1社だけ作る」→ /generate
- **クイック生成カード**（既存機能を圧縮して残す・折りたたみの2番手カード）: サービス/人格/URL/生成ボタン・疑似進捗・**エラーバナー（再試行付き）**・4分岐遷移は現行ロジックをそのまま移植（force:false/forceLow:false、タイマー clearTimeout 3経路 — MAP `/` 節参照）
- **最近の生成5件**＋「すべて見る→/history」。**空状態（まだ履歴がありません→メールを作成するCTA）も維持**
- **削除するもの（旧KPI 4枚すべて）**: 総生成数・今月の生成・高相性率%・サービス数。理由: 日々の意思決定に使われない。総数は履歴の件数バッジ、商材数は設定>商材で見られる

### 4.2 一括送信 `/bulk-send`（最大・最後に実装・IA上の最重要画面）
- **画面上部にモード2タブ**（同一ルート内のUI状態・ルート分割はしない）: 「テンプレで一斉送信」＝**デフォルト選択**／「生成済みメールを送る」（現行モーダルを全幅パネルに昇格）
- テンプレ一斉送信側は**番号付き3ステップの縦セクション＋段階的開示**:
  - **① だれに送る** — 常時展開。宛先追加4手段の大ボタン（ラベルは現行文字列を維持）＋宛先テーブル（現行の全列・行状態・インライン編集・削除・重複スキップ文言を維持）
  - **② なにを送る／③ だれから送る** — **宛先が0件の間は折りたたみ**（1件以上で自動展開・手動開閉も可）。②=テンプレ or 直接入力＋添付＋「実際に届くメール」プレビュー、③=送信元 select（要再認証表示維持）。7±2原則を最重要画面にも適用
- **画面下部固定の送信バー**（テンプレモード時）: 「選択N件（重複M件除外）」＋**生成ボタン（進捗N/N・{{AI:}}解決と警告回収のトリガー＝安全弁#5。省略禁止）**＋中断＋要確認チェック＋予約日時＋「送信する/予約する」青ボタン
  - **要確認チェックの直下に説明1行を常設**: 「会社ごとの内容になっていない可能性のある宛先があります。内容を確認してからチェックしてください」（チェックの意味を分からず形骸化させない）
  - 生成→送信の順序関係・warnings 表示・confirm・直列ループ・cancelRef・rowStatus 状態機械は現行ロジックを完全移植（MAP ⚠️11項目すべて）
- **生成済みメール側のフッターは別仕様**: 現行モーダルどおり「予約送信/テスト送信/選択を各社へ送信」のボタン群・検索/フィルタ/genSelectable/重複バッジ/インライン編集を全幅で完全移植。モード切替でフッターごと差し替わることを明記
- Gmail未接続/テンプレ0件/添付不許可の警告バナーは①②の文脈位置に埋め込む

### 4.3 メール作成 `/generate`
- モード切替タブ「1社ずつ」「まとめて」（「1社」→「1社ずつ」は §6 記載の意図的変更）
- 左=基本設定（商材/人格/URL または **企業を選ぶ**リスト ※UIラベルに「ピッカー」等のカタカナを使わない）・右=**「詳しい設定」として初期折りたたみ**（テンプレ/トーン/文章量/CTA/固定文/追加指示。テンプレ選択時に tone/length/cta が消える現行ルール維持）
- 企業選択リストのフィルタ（メアド有無/生成状態/キーワード/商材）は**現行の select を維持**（チップ化は見送り＝テスト互換とDOM構造維持）。状態バッジの優先順（送信済み>生成済み>未生成）維持
- バッチの skipped 扱い・並列3・中止・refreshGenStatus・beforeunload は現行どおり（MAP 参照）

### 4.4 宛先集め `/collection` `/collection/companies` `/collection/search`
- サブタブ（ルート分割・現行 TabNav 継承）: 自動収集 / 企業一覧 / キーワード検索
- **自動収集**: 上=在庫サマリ（4指標・低在庫/自動停止バナー維持）、中=収集元リスト（状態ドット・paused_kind 色分け・resume/一時停止の分岐・削除confirm維持）、下=**収集元の追加3手段**: ①調べ続けるキーワードの追加フォーム ②WantedlyのURL巡回フォーム ③**「Wantedly新着を収集元に追加」ボタン（hasWantedlySource の出し分け＝重複登録防止を維持）**。タブ見出し直下に1行説明「ここに登録したものは毎日自動で集め続けます（1回だけ探すときはキーワード検索へ）」— 隣タブとの取り違え防止
- **企業一覧**: YouTube Studio「コンテンツ」式テーブル。上部=状態タブ（すべて/完了/準備中/調査できず・件数付き＝現行踏襲）＋絞り込み（送信済み・キーワード・商材＝**現行のトグル/select構造を維持**）、行=チェック（hp_url 有りのみ）＋企業名＋状態＋メール＋経路＋登録日
  - **選択バーはテーブル直上に表示**（YouTube Studio 方式。下部固定をやめモバイルの下部領域衝突を根絶）:「N社選択中 → メールを生成」（sessionStorage 受け渡し維持）
  - アクションボタン（準備中N社を調査/メール再取得/整合チェック/更新）は**現行どおり条件付きで直接表示**（メニュー集約は撤回＝ペルソナ・テスト互換の両指摘対応）。ラベルも現行文字列維持
  - URLクエリ `?status=pending` 等を読んで状態タブ初期選択（ホームカードからの導線。読むだけ・無ければ「すべて」）
  - 個社単位の「この1社だけ調査」は**対応APIが存在しないため v1 対象外**（一括ボタンが準備中全社対象であることをボタン近傍に明記）
- **キーワード検索**: 検索フォーム→進捗→結果テーブルの縦一列は現行踏襲。送信済み自動uncheck・除外フィルタ・3分岐表示（メール有/フォームのみ/未検出）・採用中バッジ・フッター2アクションは完全移植

### 4.5 履歴 `/history`
- **状態チップ = 現行7状態すべて＋「すべて」**（横スクロール可・件数付き）: すべて / 予約 N / 未送信 / 送信済み / 返信あり / 商談 / 見送り / 失敗。旧フィルタselect廃止による**絞り込み能力の退行ゼロ**を保証（アイコンは phosphor。絵文字不使用）
- 検索ボックス＋「詳しい絞り込み」折りたたみ（相性・商材＝現行select維持。＋任意で期間 今日/7日/30日 のクライアント側フィルタを追加してよい）
- テーブルは現行列構成を維持。行内ステータス変更 select・CSV出力・通算N通バッジ（domain 正規化突合）維持
- **操作列の危険度分離**: 「予約取消」=グレー系ボタン（1件の軽い取り消し）／「送信しないリストに追加」=赤系＋区切り線で分離＋別アイコン（Prohibit）＋hover/長押しで「この会社へ二度と送れなくなります」表示。**両者を同じ見た目で並べない**。名称・confirm 文言・e.preventDefault・ローカル immutable 更新は現行のまま
- URLクエリ `?status=scheduled|replied` を読んでチップ初期選択（ホームからの導線）
- 空状態は「まだ履歴がありません → メールを作成する」CTA

### 4.6 メール詳細 `/prospect/[id]`
- パンくず「履歴 ‹ 会社名」。シェル（サイドバー）は表示、ナビのアクティブは「履歴」
- **左=メール（主役）**・右=企業分析（カード・モバイルは折りたたみ＝現行踏襲）。現行の左右を入れ替え
- **下部固定アクションバー**: デスクトップ=6項目（再生成/フォローアップ/コピー/テンプレ保存＋保存/送信）、**モバイル=現行どおり4項目**（再生成/コピー/保存/送信。フォローアップ・テンプレ保存はデスクトップのみ＝現行仕様を維持）
- 送信ボタンは青・1つ。canSend 3条件・2段階 ack・お断り confirm（法令文言そのまま）・送信前自動保存の順序は完全移植。**confirm の直前に画面上で平易な1文（「今から◯◯社へ実際にメールを送ります」）を表示**してから confirm を出す（confirm 文言自体は不変）
- テストモード/お断りバナーは本文カード直上（現行同等の視認性）

### 4.7 設定 5ページ `/settings/*`
- サブタブ: 全般 / テンプレート / 商材 / 人格 / 送信しないリスト
- **全般はカード7枚**（現行7セクション全てに居場所を明示）: ①接続（Gmail: アカウント行・日次上限・日程調整URL・切断）②**配信停止受付アドレス**（sender_email・独立PUT区分のまま）③検索設定（API/スクレイピング・Serperキー）④既定値（サービス/人格）⑤外観（テーマ/アクセント）⑥アカウント（アクセス保護状態表示・ログアウト）⑦危険ゾーン（履歴全削除・赤枠隔離・マジック文字列confirm維持）
- APIキー非表示・部分更新3系統・draft 管理・https必須/整数バリデーションは完全移植
- **テンプレート/商材/人格/送信しないリスト**: 現行構成を踏襲し視覚のみ刷新（テンプレの allow_attachments 連動グレーアウト・2段保存・hybrid 互換、人格・商材の削除confirm、suppressions の法令バナー最上部固定を維持）

### 4.8 ログイン `/login`
- 中央カード1枚（ロゴ・パスワード・ログイン）。シェル無し。useSearchParams 不使用・オープンリダイレクト検証・replace+refresh を完全移植。視覚のみ刷新

## 5. 認知アクセシビリティ原則（全ページ共通の合格基準）

1. **1画面1主目的**: 各画面の主ボタンは1つだけ青い大ボタン
2. **次にやることが常に見える**: 空状態・完了状態に必ず「次はこれ」ボタン
3. **新規文言は専門用語ゼロ**（既存文言の変更は §6 経由のみ。例: 新設ラベルに「ピッカー」「メンテナンス」等を使わない）
4. **状態は3点セット**（色＋アイコン＋短い日本語）
5. **破壊的操作は隔離＋重さで差別化**: 軽い取り消し（グレー）と重い禁止・削除（赤＋分離＋影響説明）を同じ見た目にしない。confirm 文言は現行維持、**confirm 前に画面上の平易な1文を追加**
6. **段階的開示**: 初見で見える要素数を画面あたり7±2ブロックに（/bulk-send 含む全画面に適用）
7. **待ち時間の見える化**: 進捗バー・N/N カウント維持
8. **やり直せる安心**: 中断・予約取消・除外（非破壊）の存在を隠さない

## 6. ラベル契約・DOM構造の変更点（smoke テストへの影響・意図的変更のみ）

| # | 変更 | 影響するテスト（想定） | 対応 |
|---|---|---|---|
| 1 | ナビ「生成」→「メール作成」 | nav.spec C1/C2 | ラベル1行更新 |
| 2 | ナビ「収集」→「宛先集め」 | nav.spec C1/C2 | 同上 |
| 3 | ナビ「設定」遷移先 /settings/templates → /settings | nav.spec C2（期待URL） | 期待URL1行更新 |
| 4 | サブタブ「在庫と自動収集」→「自動収集」 | collection 系 spec の見出し/タブ参照 | ラベル更新 |
| 5 | /generate モード「1社」→「1社ずつ」 | generate.spec | ラベル更新 |
| 6 | bulk-send 生成済みモーダル→タブ内パネル | bulk-send.spec D5-D8/D13/D14 | 開閉セレクタ更新（ネットワーク契約アサートは不変） |
| 7 | history フィルタselect→状態チップ | history.spec G2 ほか | **M4実施済み。チップのアクセシブルネーム設計により spec 更新は不要だった**（テスト無変更で通過） |
| 8 | history「フィルター」→「詳しい絞り込み」 | 影響なし（specは該当ボタン名に依存せず） | M4実施済み |
| 9 | /collection ページ見出し「企業リスト」→「宛先集め」 | render/collection 系 spec に見出し参照があれば更新 | M5 で実施 |

**見送った変更（テスト・認知互換のため実施しない）**: 企業一覧アクションボタンのメニュー集約／フィルタ select 群のチップ化（generate・companies・history詳細絞り込み）／宛先追加ボタン・重複スキップ等の既存文言変更。
上記以外のラベル・placeholder は現行文字列を維持。変更したくなったら本表に追記してから行う。

## 7. 実装マイルストーン（各回で tsc＋smoke 70本全緑＋ペルソナ自己レビュー）

| M | 対象 | リスク |
|---|---|---|
| M2 | 共通シェル（サイドバー3状態/上部バー/ドロワー/テストモードバッジ/**商材スイッチャー§3.4**）＋ §6-1〜4 のテスト更新 | 低 |
| M3 | /login ＋ /settings 5ページ | 低 |
| M4 | ホーム / 履歴 / メール詳細 | 中 |
| M5 | 宛先集め3ページ / メール作成 | 中 |
| M6 | 一括送信（§6-6 のテスト更新込み・最厚レビュー） | 高 |
| M7 | 総仕上げ: 全ページ×全ビューポート（384/768/1024/1280px）×**ライト/ダーク両テーマ**の視覚QA・認知ペルソナ通し・ナレッジ蓄積（cypherone-knowledge / ui-forge） |  |

各マイルストーン終了時の自己レビュー3問: ①直感的に操作できるか（初見スタッフのつもりで）②ジャーニーに沿っているか ③頻度の高い機能ほど目立っているか。

## 8. 実装申し送りログ

- **M7視覚QA**: サイドバー現在地の「青文字×淡青地」のコントラスト実測（M3 でトグル類は前景色に修正済み。同パターンがシェル側に残存）
- **M5**: /settings サブタブを「全般」先頭に並び替え（ラベル不変・テスト影響なし）。「サービス」→「商材」改名と /collection 見出し変更は **§6 追記後に**
- **承認済みの例外**: /settings の保存ボタン青3つ（PUT3系統の独立保存のため、カード単位の主ボタンを許容＝§5-1の例外）
- **ワーカー発注の定型**: コミットに attribution（Co-Authored-By）を付けない（M3 で混入 → amend で除去済み）。テストを緑にするためのテスト書き換え禁止・§6 記載分のみ可
- **固定バーの鉄則（M4発見）**: 画面下部固定バーはサイドバー幅ぶんの左 padding（`md:pl-16 xl:pl-60` 相当）が必須。忘れるとボタンがサイドバー下に潜る（M4 で /prospect を修正済み。**M6 の一括送信バーも該当** → M6-BULKSEND-PLAN に反映済み）
- M4判断: 「フォームを開く」はアクションバーから本文カード内リンクへ移設（機能維持・6項目構成優先）。期間フィルタ（今日/7日/30日）を履歴の詳しい絞り込みに追加
- **M7結果（2026-07-26 完了）**: 視覚QA 112枚＋数値監査 → コントラスト違反 **251→0**（文字用トークン役割分離 b9508dc/4ca1a16・color-mix で8アクセント×両テーマ追従）・横スクロール0・固定バー重なり0。ペルソナ6シナリオ → BLOCKER1(履歴操作列が1280pxで画面外→sticky化)・MAJOR3(企業一覧の商材連動バグ=**selectのoptionに無い値は黙ってallに落ちる**根治/モーダルバッジ縦崩れ/警告ブロックの無言→常設バナー)・改善2(返信のGmailリンク/スクロール影) を 3eabef6 で全て修正。設計が効いた点3つ（自動収集タブの説明1行・3ステップの先読み文言・予約取消のconfirm文）も実地で確認
- 未対応と決めたもの: グラデアバター頭文字のコントラスト（計測不能・M8任意）／「AIにおまかせ（…）」の2行折返し（文言短縮が必要なため§6追記待ち）／専門用語の残り（スクレイピング・特定電子メール法等=法令文言は不変が正）
- **M6敵対レビュー（Sonnet4視点・9件検出）**: 修正3件=①モバイルで下部送信バーが③を隠す（余白を実高さ連動に）②アクティブタブ再クリックで検索が消える③企業一覧フィルタ既定値が名前一致（**KB rec:2 の entity-name-match パターンを出荷前に阻止**。ID照合できる箇所のみ連動を残す）。承認5件=生成済みタブ見出しの送信元select追加／②入力切替の見出し行配置（D9テスト互換のため）／右パネル空状態の新規1文言／折りたたみの手動操作優先ラッチ（M7ペルソナで再確認）／bulk-send.spec 内部名リネームはM7任意
- **教訓（M6運用）**: 発注書と手順書の二重管理で「商材フィルタ連動」の許可の記載が割れ、レビュアーがスコープ違反と誤検知 → 以後、許可事項は手順書側へ一元化する
- M5判断: 選択バーのボタン名は「メール生成」維持（handoff.spec の部分一致契約。§4.4 の「メールを生成」表記より実装が正）／collection系の絵文字バッジは phosphor アイコンに置換済み（語句不変・テスト影響なし）。bulk-send の絵文字は M6 では触らず M7 で判断／旧 companies に**未定義CSSトークン**（--color-fg 等＝スタイルが効いていなかった）があり既存トークンへ修正（KB起票候補）／サブタブ「キーワードで探す」の改名・並び順変更は §6 追記後の M7 任意項目
